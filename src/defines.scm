;compile in debug mode - verbose printing
(define debug #f)

;compile a standalone executable, rather than a linkable one
(define standalone #t)

;Compile with support for displays
(define with-lem1802 #t)

;Compile with support for keyboard
(define with-kb #t)

;Compile with clock support
(define with-clock #f)

;Compile with disk support
(define with-floppy #f)

